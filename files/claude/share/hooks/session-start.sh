#!/bin/bash

# PREVIOUS_VERSION=$(cat "$HOME/.claude/cache/previous-version")
#
# if [[ "$(claude --version)" != "$PREVIOUS_VERSION" ]]; then
#   echo "Version updated from $PREVIOUS_VERSION!" > /dev/tty
#   # if [ -n "$CLAUDE_ENV_FILE" ]; then
#   #   echo "export VERSION_CHANGED_FROM=$PREVIOUS_VERSION" >> "$CLAUDE_ENV_FILE"
#   # fi
# fi
#
